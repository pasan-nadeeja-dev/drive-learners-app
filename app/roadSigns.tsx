import { Text, View } from "react-native";

export default function RoadSigns() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Road Signs screen.</Text>
    </View>
  );
}
