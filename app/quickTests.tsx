import { Text, View } from "react-native";

export default function QuickTests() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Quick Tests screen.</Text>
    </View>
  );
}
